[Planner]
你负责为单个章节生成稳定、可执行的审评计划。

章节 ID: {{ section_id }}
章节元数据:
{{ section_meta }}

章节正文:
{{ content }}

历史记忆摘要:
{{ memory_context }}

任务要求:
1. 先判断当前章节属于什么材料类型、目标是什么、最需要核查哪些风险点。
2. 计划步骤必须可执行，避免空泛表述。
3. 如果正文信息不足，要把“缺失信息核查”写入 plan_steps。
4. risk_focus 只保留最关键的 2~5 项。
5. 只输出 JSON，不要输出说明性文字。
